<?php
/**
 * Paramètres des cours — déplacé dans Configuration WAM
 *
 * L'encart tarifs est désormais géré depuis :
 * Réglages → Configuration WAM → onglet Général → section Tarifs
 *
 * Ce fichier est conservé pour compatibilité mais ne fait plus rien.
 *
 * @package wamv1
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
